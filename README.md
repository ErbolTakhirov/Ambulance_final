# 🚑 Smart Ambulance Routing System

### 🧠 AI-Powered Emergency Navigation for Bishkek

Проект разработан в рамках хакатона 2025. Система "умной" маршрутизации для машин скорой помощи, которая учитывает реальные пробки, время суток и прогнозирует время прибытия с использованием Machine Learning.

![Project Demo](demo_screenshot.png) *(Добавьте скриншот сюда)*

## 🚀 Ключевые возможности

*   **Smart Routing:** Построение оптимального маршрута с учетом пробок (а не просто кратчайшего).
*   **ML Prediction:** Предсказание времени прибытия (ETA) с учетом исторических данных и текущей загруженности дорог.
*   **Traffic Visualization:** Визуализация пробок на реальной карте города (как в 2GIS/Yandex).
*   **Hospital Management:** Выбор ближайшей станции скорой помощи с учетом наличия свободных машин.
*   **Live Tracking:** Анимация движения машины по маршруту в реальном времени.

## 🛠 Технологический стек

*   **Backend:** Django, Django REST Framework
*   **AI/ML:** Scikit-learn, Numpy (для предиктивной модели)
*   **Mapping:** OpenStreetMap (OSM), OSRM (Routing Engine)
*   **Frontend:** Vanilla JS, Leaflet.js
*   **Database:** SQLite (Dev), PostgreSQL (Prod ready)

## 📦 Установка и запуск

1.  **Клонируйте репозиторий:**
    ```bash
    git clone https://github.com/your-username/ambulance-route.git
    cd ambulance-route/backend
    ```

2.  **Создайте и активируйте виртуальное окружение:**
    ```bash
    python -m venv venv
    # Windows
    .\venv\Scripts\activate
    # Mac/Linux
    source venv/bin/activate
    ```

3.  **Установите зависимости:**
    ```bash
    pip install -r requirements.txt
    ```

4.  **Запустите сервер:**
    ```bash
    python manage.py runserver
    ```

5.  **Откройте в браузере:**
    Перейдите по адресу `http://127.0.0.1:8000`

## 📊 Как работает ML-модель

Система использует `TravelTimePredictor` — гибридную модель, которая комбинирует:
1.  **Базовый расчет OSRM** (скорость свободного потока).
2.  **Коэффициенты перегрузки (Congestion Factors)**, рассчитанные на основе текущих данных о трафике.
3.  **Корректировку по времени суток** (утренние/вечерние часы пик).

Это позволяет предсказывать не точечное время, а **доверительный интервал** (например, 15-18 мин), что критически важно для экстренных служб.

---
Developed by [Your Team Name]
